
module.exports = {
    name:"interactionCreate",
    async execute(message) {
        /*const prefix = message.content.charAt(0)
        if(prefix !== "?") return 
	    const commandLine = message.content.slice(prefix.length).split(" ")[0];
        const query = message.content.split (" ")[1];
        
        const command = message.client.commands.get(commandLine);
        if (!command) return;
        try {
            await command.execute(message,query,message.content);
        } catch (error) {
            console.error(error);
            await message.reply({ content: 'something went wrong', ephemeral: true });
        }*/
    }
}